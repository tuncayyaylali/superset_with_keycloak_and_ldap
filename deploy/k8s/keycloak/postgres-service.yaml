apiVersion: v1
kind: Service
metadata:
  name: keycloak-db
  namespace: superset-bi
  labels:
    app: keycloak-db
    app.kubernetes.io/component: keycloak-db
    app.kubernetes.io/part-of: bi-platform
spec:
  type: ClusterIP
  selector:
    app: keycloak-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP

